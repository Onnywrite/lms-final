package daemon

func main() {
	/*
		cfg := config.MustLoadDaemon()

		application := app.NewDaemon()
		go application.MustStart()

		// graceful shutdown
		shutdown := make(chan os.Signal, 1)
		signal.Notify(shutdown, syscall.SIGTERM, syscall.SIGINT)

		<-shutdown

		application.Stop()
	*/
}
